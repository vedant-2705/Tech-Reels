/**
 * @module common/dto/message-response.dto
 * @description
 * A simple DTO for responses that only contain a message string.
 * Used across the application for consistent response formatting.
 */

export interface MessageResponseDto {
    message: string;
}
